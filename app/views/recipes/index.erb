<div id="home">
  <h2 class='header-text'>Recent Recipes</h2>
  <div class='recipe-cards-container container-fluid'>
    <% @recipes.each do |recipe| %>
      <%= render partial: 'partials/recipe_card', locals: {recipe: recipe} %>
    <% end %>
  </div>
</div>
