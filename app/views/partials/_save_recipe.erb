<% if current_user.recipes.include? recipe %>
  <% button_state = 'Saved!' %>
<% else %>
  <% button_state = 'Save To Recipe Book' %>
<% end %>

<%= link_to 'Save', recipe_books_path, {class: 'save-recipe-btn btn btn-lg btn-success', id: "#{recipe.id} #{current_user_id}"} %>
